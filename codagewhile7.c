#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <sys/random.h>

void quotientbetween(int,int);

int main(){
    quotientbetween(-5,5);
}

void quotientbetween (int j,int k){
    int quotient=1;
    for (int i = j; i <= k; i++)
    {
        if (i==0){
        }
        else{
        produit=i*produit;
        }
    }
    printf("%d",produit);
    
}
    

